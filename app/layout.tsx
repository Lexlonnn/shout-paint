import type { Metadata } from 'next'
import { GeistSans } from 'geist/font/sans'
import { GeistMono } from 'geist/font/mono'
import './globals.css'

export const metadata: Metadata = {
  title: 'Shout-Figma',
  description: 'Shout-Paint is an innovative web-based painting application that revolutionizes digital art creation by using your voice as a color controller. Built with Next.js and featuring a sleek Figma-inspired interface, this interactive canvas responds to the intensity of your voice, automatically selecting colors based on how loud you speak or shout.',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <head>
        <style>{`
html {
  font-family: ${GeistSans.style.fontFamily};
  --font-sans: ${GeistSans.variable};
  --font-mono: ${GeistMono.variable};
}
        `}</style>
      </head>
      <body>{children}</body>
    </html>
  )
}
